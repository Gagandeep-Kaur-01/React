/*
For..Of :
is an alternative for both for...in and forEach() 
and loops iterable data structures like Arrays, Maps, Sets, and strings.
*/

const arr = ['one', 'two', 'three'];

for(const a of arr) {
    console.log(a);
}
